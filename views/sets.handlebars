<div class='row'>
  <div class='col s12 m6'>
    <div class='card blue-grey darken-1 scrollable-card center-content transparent-card'>
      <div class='card-content white-text'>
        <span class='card-title'></span>
        {{#each sets as |set|}}
        
          <h1 class="venue">{{set.venueName}}</h1>

          <h4 class="tour">{{set.tourName}}</h4>

          <p class="where">{{set.city}}</p>
          <p class="where">{{set.country}}</p>
          


          <h4 class="set-heading">Set One</h4>
          {{#each set.setOneSongs as |st1|}}
            <p>{{st1}}</p>
          {{/each}}


          <h4 class="set-heading">Set Two</h4>
          {{#each set.setTwoSongs as |st2|}}
            <p>{{st2}}</p>
          {{/each}}
        {{/each}}
      </div>
      <div class='card-action'>
     </div>
    </div>
  </div>
</div>